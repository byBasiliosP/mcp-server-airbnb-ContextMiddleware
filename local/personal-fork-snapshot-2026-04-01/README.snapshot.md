# Personal Snapshot

This directory preserves the working fork implementation as of `2026-04-01` before the repository was reshaped for broader developer consumption.

Use it as a local reference for the opinionated agent workflow version:

- context preparation
- contextual search
- result reconciliation
- compact agent responses
- anonymous context caching

The active developer-facing package now lives under `packages/mcp-server-public`.
